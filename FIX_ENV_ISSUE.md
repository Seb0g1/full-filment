# Исправление проблемы с загрузкой .env файла

## Проблема

Файл `server/.env` существует, но сервер не видит переменные окружения.

## 🔍 Диагностика

### 1. Проверьте, что файл существует и находится в правильной директории

На сервере выполните:

```bash
cd /path/to/full-filment
ls -la server/.env
```

Должен показать файл. Если файла нет, создайте его.

### 2. Проверьте содержимое файла

```bash
cat server/.env
```

**Важно:** Убедитесь, что:
- ✅ Нет пробелов вокруг знака `=`
- ✅ Нет кавычек вокруг значений (или они правильно закрыты)
- ✅ Нет пустых строк с пробелами
- ✅ Каждая переменная на отдельной строке

### 3. Проверьте формат файла

Файл должен выглядеть так:

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_GROUP_CHAT_ID=-1001234567890
PORT=3001
TELEGRAM_TOPIC_CHAT_CLIENT=0
TELEGRAM_TOPIC_CALCULATOR=0
TELEGRAM_TOPIC_CONTACT_FORM=0
```

**Неправильно:**
```env
TELEGRAM_BOT_TOKEN = 123456789:ABC...  # Пробелы вокруг =
TELEGRAM_BOT_TOKEN="123456789:ABC..."  # Кавычки (можно, но не обязательно)
TELEGRAM_BOT_TOKEN=123456789:ABC... # комментарий  # Комментарий на той же строке
```

**Правильно:**
```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
# Комментарий на отдельной строке
```

### 4. Проверьте кодировку файла

Файл должен быть в UTF-8 без BOM:

```bash
file server/.env
```

Если кодировка неправильная, пересоздайте файл:

```bash
nano server/.env
# Скопируйте содержимое заново
```

### 5. Проверьте права доступа

```bash
ls -la server/.env
```

Файл должен быть читаемым:

```bash
chmod 644 server/.env
```

## 🔧 Решение

### Вариант 1: Пересоздать файл .env

1. Удалите старый файл:
```bash
rm server/.env
```

2. Создайте новый:
```bash
nano server/.env
```

3. Добавьте содержимое (без пробелов вокруг `=`):
```env
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
TELEGRAM_GROUP_CHAT_ID=-ваш_chat_id_здесь
PORT=3001
TELEGRAM_TOPIC_CHAT_CLIENT=0
TELEGRAM_TOPIC_CALCULATOR=0
TELEGRAM_TOPIC_CONTACT_FORM=0
```

4. Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Вариант 2: Исправить существующий файл

```bash
nano server/.env
```

Убедитесь, что:
- Нет пробелов вокруг `=`
- Каждая строка заканчивается переводом строки
- Нет лишних символов

### Вариант 3: Использовать echo для создания файла

```bash
cat > server/.env << 'EOF'
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
TELEGRAM_GROUP_CHAT_ID=-ваш_chat_id_здесь
PORT=3001
TELEGRAM_TOPIC_CHAT_CLIENT=0
TELEGRAM_TOPIC_CALCULATOR=0
TELEGRAM_TOPIC_CONTACT_FORM=0
EOF
```

## 🔄 Перезапуск сервера

После исправления файла:

```bash
# Остановить PM2 процесс
pm2 stop fulfillment-bot

# Удалить из PM2 (чтобы очистить кэш переменных окружения)
pm2 delete fulfillment-bot

# Запустить заново
pm2 start ecosystem.config.js

# Или если используете другой способ запуска:
pm2 restart all --update-env
```

**Важно:** Используйте `--update-env` флаг, чтобы PM2 перезагрузил переменные окружения!

## ✅ Проверка

После перезапуска проверьте логи:

```bash
pm2 logs fulfillment-bot --lines 50
```

Вы должны увидеть:
```
✅ .env файл загружен из: /path/to/server/.env
🔍 Отладка переменных окружения:
   TELEGRAM_BOT_TOKEN: ✅ Установлен (123456789...)
   TELEGRAM_GROUP_CHAT_ID: ✅ Установлен (-1001234567890)
✅ Telegram bot initialized
✅ Telegram bot is ready to receive messages!
```

## 🆘 Если все еще не работает

### Проверьте ecosystem.config.js

Убедитесь, что PM2 не переопределяет переменные окружения:

```bash
cat ecosystem.config.js
```

Если там есть `env`, убедитесь, что он не перезаписывает переменные из .env.

### Проверьте, откуда запускается сервер

```bash
pm2 info fulfillment-bot
```

Проверьте поле `cwd` (current working directory) - сервер должен запускаться из корня проекта.

### Альтернатива: Установить переменные в PM2

Можно установить переменные напрямую в PM2:

```bash
pm2 set TELEGRAM_BOT_TOKEN "ваш_токен"
pm2 set TELEGRAM_GROUP_CHAT_ID "-ваш_chat_id"
pm2 restart fulfillment-bot
```

Но лучше использовать .env файл.

## 📝 Пример правильного .env файла

```env
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_GROUP_CHAT_ID=-1001234567890
PORT=3001
TELEGRAM_TOPIC_CHAT_CLIENT=0
TELEGRAM_TOPIC_CALCULATOR=0
TELEGRAM_TOPIC_CONTACT_FORM=0
```

**Правила:**
- ✅ Без пробелов вокруг `=`
- ✅ Без кавычек (или с правильными кавычками)
- ✅ Каждая переменная на отдельной строке
- ✅ UTF-8 кодировка
- ✅ Файл должен заканчиваться переводом строки

