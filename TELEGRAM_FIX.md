# 🔧 Исправление проблемы с Telegram ботом

## Что было исправлено:

1. ✅ Исправлена загрузка `.env` файла - теперь он правильно загружается из папки `server/`
2. ✅ Улучшена обработка ошибок при инициализации бота
3. ✅ Добавлены подробные сообщения об ошибках для диагностики

## 📋 Что нужно сделать:

### 1. Создайте файл `.env` в папке `server/`

Создайте файл `server/.env` со следующим содержимым:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
TELEGRAM_GROUP_CHAT_ID=-123456789
PORT=3001
```

### 2. Получите токен бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Скопируйте токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 3. Получите ID группы

1. Создайте группу в Telegram
2. Добавьте бота в группу
3. Сделайте бота администратором (рекомендуется)
4. Отправьте любое сообщение в группу
5. Перейдите по ссылке (замените `<ВАШ_ТОКЕН>` на токен из шага 2):
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
6. Найдите в ответе `"chat":{"id":-123456789}` - это ID группы (отрицательное число)

### 4. Запустите сервер

```bash
cd server
npm install  # если еще не установлены зависимости
npm run dev
```

### 5. Проверьте работу

При запуске сервера вы должны увидеть:
```
✅ Telegram bot is ready to receive messages!
```

Если видите предупреждения, проверьте:
- Правильность токена бота
- Правильность ID группы (должен быть отрицательным числом)
- Что бот добавлен в группу и является администратором

## ❓ Частые проблемы:

### "Telegram bot not initialized - no token provided"
- Создайте файл `server/.env` с переменными `TELEGRAM_BOT_TOKEN` и `TELEGRAM_GROUP_CHAT_ID`

### "Invalid bot token"
- Проверьте, что токен скопирован полностью
- Убедитесь, что нет лишних пробелов в `.env` файле

### "Бот не добавлен в группу"
- Убедитесь, что бот добавлен в группу
- Проверьте, что ID группы правильный (отрицательное число)

### Сообщения не отправляются
- Проверьте логи сервера - там будут подробные сообщения об ошибках
- Убедитесь, что сервер запущен на порту 3001
- Проверьте, что фронтенд правильно настроен (см. `CHAT_SETUP.md`)

## 📝 Дополнительная информация:

- Подробная инструкция: `CHAT_SETUP.md`
- Настройка топиков: `TELEGRAM_TOPICS.md`
- Настройка для продакшена: `DEPLOY.md`

