# 🚀 Быстрый старт

## 1. Загрузка на GitHub (с Windows)

### Вариант А: Через PowerShell скрипт (рекомендуется)

```powershell
# Откройте PowerShell в директории проекта
cd "C:\Users\Хуйню придумал\Downloads\FulFilment (1)"

# Запустите скрипт
.\git-init.ps1

# После успешной инициализации загрузите на GitHub
git push -u origin main
```

### Вариант Б: Вручную

Следуйте инструкциям в файле `GIT_SETUP.md`

## 2. Настройка на сервере (Linux)

### Шаг 1: Клонирование и первоначальная настройка

```bash
# Клонируйте репозиторий
git clone https://github.com/Seb0g1/full-filment.git
cd full-filment

# Запустите скрипт первоначальной настройки
chmod +x setup.sh deploy.sh
./setup.sh
```

### Шаг 2: Настройка переменных окружения

Создайте файл `server/.env`:

```bash
nano server/.env
```

Добавьте:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_бота
TELEGRAM_GROUP_CHAT_ID=ваш_chat_id
PORT=3001
TELEGRAM_TOPIC_CHAT_CLIENT=0
TELEGRAM_TOPIC_CALCULATOR=0
TELEGRAM_TOPIC_CONTACT_FORM=0
```

### Шаг 3: Настройка Nginx

```bash
# Скопируйте конфигурацию
sudo cp nginx.example.conf /etc/nginx/sites-available/sakoo.ru

# Отредактируйте путь к проекту
sudo nano /etc/nginx/sites-available/sakoo.ru
# Найдите строку: root /path/to/your/project/dist;
# Замените на реальный путь, например: root /home/user/full-filment/dist;

# Активируйте конфигурацию
sudo ln -s /etc/nginx/sites-available/sakoo.ru /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### Шаг 4: Запуск

```bash
# Запустите деплой (соберет проект и запустит сервер)
./deploy.sh
```

## 3. Обновление проекта

После изменений на GitHub, на сервере просто выполните:

```bash
cd /path/to/full-filment
./deploy.sh
```

Скрипт автоматически:
- ✅ Обновит код с GitHub
- ✅ Установит зависимости
- ✅ Соберет frontend
- ✅ Перезапустит backend сервер

## 4. Управление процессами

```bash
# Статус
pm2 status

# Логи
pm2 logs fulfillment-bot

# Перезапуск
pm2 restart fulfillment-bot

# Остановка
pm2 stop fulfillment-bot
```

## 📚 Дополнительная документация

- `DEPLOY.md` - Подробная инструкция по деплою
- `GIT_SETUP.md` - Настройка Git и GitHub
- `README.md` - Общая информация о проекте
- `CHAT_SETUP.md` - Настройка Telegram чата

