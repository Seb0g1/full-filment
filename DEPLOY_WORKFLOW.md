# 🔄 Рабочий процесс: Загрузка на GitHub и обновление на сервере

## 📤 Шаг 1: Загрузка изменений на GitHub (с локального компьютера)

### Вариант 1: Использовать готовый скрипт

```powershell
.\push-changes.ps1
```

### Вариант 2: Вручную

Откройте PowerShell в директории проекта и выполните:

```powershell
# 1. Проверить статус
git status

# 2. Добавить измененные файлы
git add src/utils/chatService.js
git add src/utils/telegram.js
git add server/server.js
git add DEPLOY.md
git add ecosystem.config.js
git add FIX_ENV_ISSUE.md
git add SERVER_TELEGRAM_SETUP.md
git add DEPLOY_WORKFLOW.md

# 3. Создать коммит
git commit -m "Fix: Исправлена ошибка ERR_CONNECTION_REFUSED и добавлена отладка для .env"

# 4. Отправить на GitHub
git push origin main
```

## 📥 Шаг 2: Обновление на сервере

### Вариант 1: Использовать скрипт deploy.sh (рекомендуется)

Подключитесь к серверу по SSH и выполните:

```bash
cd /path/to/full-filment
./deploy.sh
```

Скрипт автоматически:
- ✅ Обновит код с GitHub (`git pull`)
- ✅ Установит зависимости
- ✅ Соберет frontend
- ✅ Перезапустит backend сервер

### Вариант 2: Вручную

```bash
# 1. Подключиться к серверу
ssh user@your-server.com

# 2. Перейти в директорию проекта
cd /path/to/full-filment

# 3. Обновить код с GitHub
git pull origin main

# 4. Установить зависимости
npm install
cd server && npm install && cd ..

# 5. Собрать frontend
npm run build

# 6. Перезапустить backend сервер
pm2 restart fulfillment-bot --update-env
```

## 🔍 Проверка после обновления

### На сервере проверьте логи:

```bash
pm2 logs fulfillment-bot --lines 50
```

Должны увидеть:
```
✅ .env файл загружен из: /path/to/server/.env
🔍 Отладка переменных окружения:
   TELEGRAM_BOT_TOKEN: ✅ Установлен (...)
   TELEGRAM_GROUP_CHAT_ID: ✅ Установлен (...)
✅ Telegram bot initialized
```

### Проверьте статус PM2:

```bash
pm2 status
```

### Проверьте работу API:

```bash
curl http://localhost:3001/health
```

## 🚀 Быстрый деплой (одна команда на сервере)

Если у вас настроен SSH доступ, можно обновить сервер одной командой с локального компьютера:

```powershell
ssh user@your-server.com "cd /path/to/full-filment && ./deploy.sh"
```

## 📝 Полный процесс (пример)

### На локальном компьютере:

```powershell
# 1. Добавить изменения
git add .
git commit -m "Описание изменений"
git push origin main
```

### На сервере:

```bash
# 2. Обновить и перезапустить
cd /path/to/full-filment
./deploy.sh
```

## ⚠️ Важные замечания

1. **Файл `server/.env`** не должен быть в git (он в `.gitignore`)
   - Убедитесь, что на сервере файл `.env` существует и правильно настроен
   - После `git pull` файл `.env` не будет перезаписан

2. **Переменные окружения в PM2**
   - Используйте `--update-env` при перезапуске, чтобы обновить переменные из `.env`

3. **Проверка перед деплоем**
   - Убедитесь, что код работает локально
   - Проверьте, что все зависимости установлены

## 🆘 Если что-то пошло не так

### Откатить изменения на сервере:

```bash
cd /path/to/full-filment
git reset --hard HEAD~1  # Откатить последний коммит
git pull origin main     # Или вернуться к последней версии
pm2 restart fulfillment-bot
```

### Посмотреть историю коммитов:

```bash
git log --oneline -10
```

### Вернуться к конкретному коммиту:

```bash
git checkout <commit-hash>
pm2 restart fulfillment-bot
```

