# Настройка Telegram бота на сервере

## ⚠️ Проблема

В логах сервера видно:
```
⚠️  Telegram bot token or group chat ID not configured!
⚠️  Telegram bot not initialized - no token provided
```

Это означает, что на сервере не настроен файл `server/.env` с токеном бота.

## 🚀 Быстрое решение

### 1. Подключитесь к серверу по SSH

```bash
ssh user@your-server.com
```

### 2. Перейдите в директорию проекта

```bash
cd /path/to/full-filment
```

### 3. Создайте файл `server/.env`

```bash
nano server/.env
```

### 4. Добавьте в файл следующие строки:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_botfather
TELEGRAM_GROUP_CHAT_ID=-123456789
PORT=3001
TELEGRAM_TOPIC_CHAT_CLIENT=0
TELEGRAM_TOPIC_CALCULATOR=0
TELEGRAM_TOPIC_CONTACT_FORM=0
```

**Важно:** Замените `ваш_токен_от_botfather` и `-123456789` на реальные значения!

### 5. Сохраните файл

В nano: нажмите `Ctrl+O`, затем `Enter`, затем `Ctrl+X`

### 6. Перезапустите сервер

```bash
pm2 restart fulfillment-bot
```

Или если используете другой процесс-менеджер:
```bash
pm2 restart all
```

## 📋 Как получить токен бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Скопируйте токен бота (выглядит как `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## 📋 Как получить Chat ID группы

1. Создайте группу в Telegram
2. Добавьте бота в группу
3. Сделайте бота администратором (рекомендуется)
4. Отправьте любое сообщение в группу
5. Перейдите по ссылке в браузере (замените `<ВАШ_ТОКЕН>` на токен из шага выше):
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
6. Найдите в ответе `"chat":{"id":-123456789}` - это ID группы (отрицательное число)

## ✅ Проверка

После перезапуска проверьте логи:

```bash
pm2 logs fulfillment-bot
```

Вы должны увидеть:
```
✅ Telegram bot initialized
👥 Group chat ID: -123456789
✅ Telegram bot is ready to receive messages!
```

## 🔒 Безопасность

⚠️ **Важно!** Файл `server/.env` содержит секретные данные:
- Не добавляйте его в git (он уже в `.gitignore`)
- Не публикуйте токен бота
- Храните файл в безопасном месте

## 📝 Пример полного файла `.env`

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_GROUP_CHAT_ID=-1001234567890

# Server Configuration
PORT=3001

# Telegram Topics (опционально, если группа с топиками)
TELEGRAM_TOPIC_CHAT_CLIENT=0
TELEGRAM_TOPIC_CALCULATOR=0
TELEGRAM_TOPIC_CONTACT_FORM=0
```

## 🆘 Если не работает

1. Проверьте, что файл создан в правильной директории: `server/.env`
2. Проверьте, что нет лишних пробелов в значениях
3. Проверьте токен бота - он должен начинаться с цифр и содержать двоеточие
4. Проверьте Chat ID - для группы это отрицательное число
5. Убедитесь, что бот добавлен в группу
6. Проверьте логи: `pm2 logs fulfillment-bot --lines 50`

